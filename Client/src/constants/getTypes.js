const NAV_TYPES = Object.freeze({
  ADMIN: 'Admin',
  PUBLIC: 'Public',
});

const VIEW_TYPES = Object.freeze({
  DEFAULT: 'Default',
  RESIDENT: 'Resident',
  CHARTER: 'CHARTER',
});

export {NAV_TYPES, VIEW_TYPES};
